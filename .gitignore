# Virtual environments
.venv/
venv/
env/

# Datasets / fixtures (per AGENTS.md: data/ is gitignored)
data/

# Python bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Test / type / coverage caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS cruft
.DS_Store
Thumbs.db

.claude/

# Secrets — API keys for the benchmark VLM baselines. NEVER commit. Use .env.example
# as the template; the real .env stays local.
.env
.env.*
!.env.example

# M5 benchmark: cached VLM responses (can be large; keyed by rendered-image bytes).
# The committed deliverable is benchmark/results/benchmark_results.json + the README table.
benchmark/_vlm_cache/
# Local critique/overlay + benchmark render artifacts written to the repo root.
artstockfish_*overlay.png
artstockfish_*overlay.svg
artstockfish_bench_*.png

# AI agent guidance / planning docs — kept local only.
AGENTS.md
CLAUDE.md
ART_STOCKFISH_SPEC.md
IMPLEMENTATION_PLAN.md
DECISIONS.md
REAL_SKETCH_VALIDATION.md
